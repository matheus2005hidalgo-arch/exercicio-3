let numero = Number (prompt("Digite um número"));

let resultado = 
        numero > 0 ? "positivo" :
        numero < 0 ? "Negado" :
                     "Zero" ;

    alert(`o Número é: ${resultado}`);